destination: docs

reference:
  - title: "Classes"
    contents:
      - ip_address
      - ip_network
  - title: "Binary representation"
    contents:
      - packed
      - unpack
      - as_binary
      - from_binary
  - title: "Address spaces"
    contents:
      - is_ipv4
      - is_ipv6
      - max_prefix_length
      - is_multicast
      - is_unspecified
      - is_loopback
      - is_link_local
  - title: "Network membership"
    contents:
      - prefix_length
      - num_addresses
      - network_address
      - broadcast_address
      - netmask
      - hostmask
      - seq.ip_network
      - hosts
      - sample_ip
      - is_within
      - is_within_any
      - is_subnet
      - is_supernet
